//
// 작성자 : 김병조
// class : SubscriptView
// 설명 : createDrawerNavigator에 들어갈 screen 구현, 아직 기본적인 UI는 정하지 않음.
// 받아야 할 props : 없음.

import React, { Component } from 'react'
import { View, Text } from 'react-native'

class SubscriptView extends Component {
  render () {
    return (
      <View>
        <Text>Subscript</Text>
      </View>
    )
  }
}

export default SubscriptView
